{{- if not .Values.jwt.access.secretName }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "auth-core.jwt.access.secretName" . }}
  labels:
    app: {{ .Release.Name }}
type: Opaque
data:
  public: {{ .Values.jwt.access.public | default (randAlphaNum 32) | b64enc }}
  private: {{ .Values.jwt.access.private | default (randAlphaNum 64) | b64enc }}
{{- end }}

