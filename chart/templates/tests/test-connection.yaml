apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-core.fullname" . }}-test-connection"
  labels:
    {{- include "auth-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
